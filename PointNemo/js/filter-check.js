/* function clearAllFilters() {
    // Get references to the original and target containers
    var originalContainer = document.getElementById('filtersContainer');
    var targetContainer = document.getElementById('selectedFilters');
    
    // Move all children from the target container back to the original container
    while (targetContainer.firstChild) {
        originalContainer.appendChild(targetContainer.firstChild);
    }
} */